export class User {
    id: number;
    created_timestamp: number;
    username: string;
    enabled: boolean;
    email_verified: boolean;
    firstName: string;
    lastName: string;
    email: string;
    access: string;
    role: string;

    // constructor(username: string, firstName: string, lastName: string, email: string, role: string) {
    //     this.username = username;
    //     this.firstName = firstName;
    //     this.lastName = lastName;
    //     this.email = email;
    //     this.role = role;
    // }

}